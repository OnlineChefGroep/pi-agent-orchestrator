name: Disabled product-film finalizer

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; product-film verification runs in remotion-showcase.yml"
