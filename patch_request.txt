Ah, wait...
The code review says:
"it fails to add the provided invocation block (// CVE-002 FIX: Validate agent config before adding...) at line 143... Because the validateAgentConfig helper is never actually called during the agent loading flow in loadCustomAgents, the newly added validation rules are practically useless."

But when I look at `src/custom-agents.ts`, `validateAgentConfig` IS called!
Lines 141-146 are:
```typescript
    // CVE-002 FIX: Validate agent config before adding
    const validationErrors = validateAgentConfig(name, config);
    if (validationErrors.length > 0) {
      emitTelemetry("agent:validation-failed", { name, errors: validationErrors });
      // Disable agent with validation errors (don't skip entirely - let user see it)
      config.enabled = false;
    }
```
Why is the code reviewer saying it's not there?
Wait! In the diff for `src/custom-agents.ts` from my commit, did I accidentally remove it?
Let me look at `git show :src/custom-agents.ts`.
